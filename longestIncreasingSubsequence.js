/**
 * @param {number[]} nums
 * @return {number}
 */
var findLengthOfLCIS = function(nums) {

  // use two pointers (still and mover), start at 0
  // increment mover until current index is lower than previous index
  // if decrease
  //    move still to mover index
  //    take max of current and max
  //    reset counter index
  // return max

};